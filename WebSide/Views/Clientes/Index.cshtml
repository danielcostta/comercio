@model IEnumerable<WebSide.Models.Cliente>

@{
    ViewBag.Title = "Index";
}

<h2>Cadastro de clientes</h2>


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeCliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CPF)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CPF)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCliente }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDCliente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCliente })
        </td>
    </tr>
}

</table>

<br /> <br />

@Html.ActionLink("Cadastro de estados", "Index", "Estados") <br />
@Html.ActionLink("Cadastro de cidades", "Index", "Cidades") <br />
@Html.ActionLink("Cadastro de enderecos", "Index", "Enderecoes") <br />
