@model IEnumerable<WebSide.Models.Estado>

@{
    ViewBag.Title = "Index";
}

<h2>Cadastro de estados</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeEstado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeEstado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDEstado }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDEstado }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDEstado })
        </td>
    </tr>
}

</table>

<br /> <br />

@Html.ActionLink("Cadastro de clientes", "Index", "Clientes") <br />
@Html.ActionLink("Cadastro de cidades", "Index", "Cidades") <br />
@Html.ActionLink("Cadastro de enderecos", "Index", "Enderecoes") <br />
